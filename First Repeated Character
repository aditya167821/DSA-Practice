char repeatedCharacter(string &str)
{
    int n = str.size();

    for (int i = 0; i < n; i++)
    {
        for (int j = i + 1; j < n; j++)
        {
            if (str[i] == str[j])
            {
                return str[i];
            }
        }
    }

    return '%';
}
